#incluude <stdio.h>
int main()
{
    char b;
    printf("Enter a character");
    scanf("%b",&b);

    if( (b>='a' && c<='z') || (b>='A' && b<='Z'))
        printf("%b is an alphabet.",c);
    else
        printf("%b is not an alphabet.",c);

    return 0;
}
